# Environment Configuration for Docker Deployment

# Node Environment
NODE_ENV=production

# MongoDB Connection
# For cloud MongoDB (MongoDB Atlas):
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?appName=appname

# For local MongoDB container (uncomment mongodb service in docker-compose.yml):
# MONGODB_URI=mongodb://admin:password@mongodb:27017/fieldnotes?authSource=admin

# JWT Secret (IMPORTANT: Change this to a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-32-characters-long

# API Port
PORT=7001

# Database Mode
USE_MOCK_DB=false

# MongoDB Root Password (if using local MongoDB container)
# MONGO_ROOT_PASSWORD=changeme
